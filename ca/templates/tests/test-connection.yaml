apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ca.fullname" . }}-test-connection"
  labels:
    {{- include "ca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
